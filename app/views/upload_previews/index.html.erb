<h1>Listing upload_previews</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @upload_previews.each do |upload_preview| %>
  <tr>
    <td><%= link_to 'Show', upload_preview %></td>
    <td><%= link_to 'Edit', edit_upload_preview_path(upload_preview) %></td>
    <td><%= link_to 'Destroy', upload_preview, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Upload preview', new_upload_preview_path %>
